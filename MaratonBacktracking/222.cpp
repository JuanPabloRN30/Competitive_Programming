#include <bits/stdc++.h>

using namespace std;

int main()
{
  double distance;
  scanf("%lf",&distance);
  double capacity,miles,cost;
  int number_stations;
  scanf("%lf%lf%lf%d",&capacity,&miles,&cost,&number_stations);
  for(int i = 0 ; i < number_stations ; i++)
  {
    
  }

  return 0;
}
